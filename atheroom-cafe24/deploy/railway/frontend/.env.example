# Set before Docker build: the existing Dockerfile consumes BACKEND_URL as ARG.
BACKEND_URL=http://${{backend.RAILWAY_PRIVATE_DOMAIN}}:8000
HOSTNAME=0.0.0.0
PORT=3000
NODE_ENV=production
NEXT_TELEMETRY_DISABLED=1
